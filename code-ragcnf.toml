# code-rag configuration file
# Place this file in your project root or in ~/.config/code-rag/code-ragcnf.toml

# Path to the LanceDB database
# db_path = "./.lancedb"

# Default path to index if not specified in CLI
# default_index_path = "."

# Default number of search results to return
default_limit = 10

# Server configuration
# server_host = "127.0.0.1"
# server_port = 3000

# Global exclusions (paths containing these strings will be skipped)
# exclusions = ["target", "node_modules", ".git"]
